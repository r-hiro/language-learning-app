# Codex引き継ぎ指示書

## 1. 目的

このファイルは、現在の語学学習アプリを別PC上のCodexで再現・継続開発するための指示書である。

最も再現性が高い方法は、このプロジェクトフォルダ内のファイル一式を別PCへコピーし、この指示書をCodexへ渡して作業を再開することである。

## 2. プロジェクト概要

日本語・英語・韓国語の単語を学習するスマートフォン向けWebアプリ。

利用者は基準言語を日本語・英語・韓国語から選び、基準言語以外の1言語または2言語の正しい訳語を選択する。2言語モードでは、回答対象の2言語に同じ意味を持つ5つの候補を表示し、それぞれの言語内で表示位置をランダムにする。

アプリは現在、静的なHTML/CSS/JavaScriptで構成されており、ブラウザで `index.html` を開くことで動作する。

## 3. 別PCへ移すべきファイル

以下のファイルとフォルダを同じ構成でコピーする。

```text
語学/
  index.html
  styles.css
  app.js
  概要設計書.md
  キャラクター設計書.md
  Codex引き継ぎ指示書.md
  assets/
    characters-concept-traditional.png
    characters-concept.png
  data/
    README.md
    words.json
    words.js
  scripts/
    generate-words.ps1
```

## 4. Codexへの最初の指示例

別PCでCodexを起動したら、以下のように依頼する。

```text
このフォルダは、日本語・英語・韓国語の単語学習Webアプリです。
まず Codex引き継ぎ指示書.md、概要設計書.md、キャラクター設計書.md を読んで、現在の実装状態を把握してください。
既存の index.html、styles.css、app.js、data/words.js、data/words.json を壊さず、既存方針に沿って続きを実装してください。
```

## 5. 現在の実装済み機能

### 5.1 ホーム画面

- 利用者名の表示
- 初期基準言語と出題数の表示
- 学習開始、苦手問題、成績、設定への導線
- 選択中の基準言語に対応したキャラクター表示
- キャラクター非表示設定

### 5.2 設定

- 利用者名
- 初期基準言語
- 初期出題数
- キャラクター表示ON/OFF
- 設定値は `localStorage` に保存

### 5.3 学習条件選択

- 基準言語選択
- 出題数選択
- 分野選択
- 回答言語数選択
  - 1言語
  - 2言語
- 1言語モードでは回答対象言語を選択可能

### 5.4 問題出題画面

- 基準言語の単語を表示
- 回答対象言語ごとに5つの候補を表示
- 2言語モードでは、2つの回答言語に同じ意味セットの候補を表示
- 各言語内で候補位置をランダム化
- 回答後、正解候補に `◎`、選択した不正解候補に `ｘ` を表示
- 回答後、すべての候補に基準言語側の単語も表示
- 例文は回答後のみ表示可能
- 例文表示時は回答対象言語の例文のみ表示
- ブラウザの音声読み上げ機能で単語を読み上げ
- クイズ画面にも小さなキャラクターとセリフを表示
- キャラクター非表示設定がONの場合は表示しない

### 5.5 基準言語に応じた表示言語切り替え

基準言語を変更した場合、以下の文言は基準言語に合わせて日本語・英語・韓国語へ切り替える。

- ホームの挨拶・説明
- クイズ画面の説明
- 操作ボタン
- 回答候補欄の見出し
- 例文表示の案内
- 音声読み上げの案内
- 結果画面の説明
- キャラクターのセリフ

### 5.6 成績管理

- 総学習回数
- 総回答数
- 総正答率
- 言語別正答率
- 苦手単語数
- 成績は `localStorage` に保存

### 5.7 苦手問題

- 間違えた単語を苦手単語として保存
- 苦手単語のみで復習可能
- 苦手問題で全回答正解した単語は苦手リストから除外

## 6. 主要ファイルの役割

### `index.html`

画面構成を定義する。

現在は以下の画面を持つ。

- ホーム
- 学習条件選択
- 苦手問題
- クイズ
- 結果
- 成績
- 設定ダイアログ

### `styles.css`

スマートフォン利用を前提にした画面デザインを定義する。

候補ボタン、クイズ画面、キャラクター表示、結果表示などの見た目を管理する。

### `app.js`

アプリの主要ロジックを持つ。

主な責務:

- 設定保存
- 画面遷移
- 問題生成
- 候補シャッフル
- 回答判定
- 例文表示制御
- 音声読み上げ
- 成績保存
- 苦手単語管理
- 基準言語に応じたUI文言切り替え

### `data/words.json`

単語データ本体。

各単語には以下を持つ。

- ID
- 分野
- 日本語
- 英語
- 韓国語
- 各言語の例文

### `data/words.js`

ブラウザから直接読み込むための単語データ。

`window.WORD_DATA` としてアプリから参照する。

### `scripts/generate-words.ps1`

`words.json` から `words.js` を生成するためのPowerShellスクリプト。

## 7. 設計書

詳細仕様は以下を参照する。

- `概要設計書.md`
- `キャラクター設計書.md`

概要設計書には、学習フロー、画面構成、データ設計、成績管理、PWA化、今後の拡張方針が含まれる。

キャラクター設計書には、日本人・英語圏・韓国人キャラクターの方針、伝統衣装、3頭身程度のかわいいデザイン、表情、セリフ方針が含まれる。

## 8. 再現時の注意

概要設計書とキャラクター設計書だけでもアプリの方向性は再現できるが、現在の実装状態を正確に再現するには不十分である。

正確に継続するには、必ず以下も渡すこと。

- `index.html`
- `styles.css`
- `app.js`
- `data/words.json`
- `data/words.js`
- `assets/` フォルダ

設計書だけを渡した場合、Codexは似たアプリを新規作成できるが、現在の画面デザイン、細かな挙動、保存キー、実装済みロジックまでは一致しない可能性が高い。

## 9. 現在の保存方式

現在はサーバーやログイン機能を持たず、ブラウザの `localStorage` に以下を保存している。

- 利用者設定: `triLangUserSettings`
- 成績情報: `triLangStats`

そのため、同じアプリを複数人で使っても、成績は各端末・各ブラウザごとに保存される。

一般公開してユーザーごとの成績をクラウド管理したい場合は、将来的にログイン機能とバックエンド、またはBaaSの導入が必要になる。

## 10. 今後の主な候補タスク

- PWA対応
  - `manifest.json`
  - service worker
  - ホーム画面追加
  - オフライン対応
- 単語データの拡充
- 分野別成績の強化
- 単語別の正答率管理
- 苦手単語の詳細画面
- キャラクター表情の切り替え強化
- UI文言の完全多言語化
- データバックアップ・エクスポート
- クラウド同期
- 広告・課金などの収益化検討

## 11. 別PCでの確認方法

1. フォルダ一式を別PCへコピーする。
2. `index.html` をブラウザで開く。
3. ホーム画面が表示されることを確認する。
4. 学習開始からクイズを開始する。
5. 1言語モードと2言語モードを確認する。
6. 基準言語を日本語・英語・韓国語に変更し、画面文言とキャラクターセリフが切り替わることを確認する。
7. 回答後に `◎` と `ｘ`、基準言語側の単語、例文ボタンが表示されることを確認する。
8. 成績画面と苦手問題画面を確認する。

## 12. Codexへの作業依頼時の注意

Codexへ依頼する際は、以下の方針を伝える。

- 既存の静的HTML/CSS/JavaScript構成を維持する。
- 大きなフレームワークは導入しない。
- スマートフォン利用を優先する。
- 既存の画面デザインや保存キーを壊さない。
- `localStorage` の既存データ構造を変更する場合は、移行処理を検討する。
- 単語データを変更した場合は、`words.json` と `words.js` の整合性を保つ。
- UIは日本語・英語・韓国語の基準言語切り替えを意識する。

